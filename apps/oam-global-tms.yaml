apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: oam-global-tms
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: oci://ghcr.io/hotosm/openaerialmap/global-tms
      chart: global-tms
      targetRevision: 0.1.1
      helm:
        releaseName: global-tms
        # Use the default values
  destination:
    server: https://kubernetes.default.svc
    namespace: eoapi
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
