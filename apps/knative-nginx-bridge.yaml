apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: knative-nginx-bridge
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/hotosm/k8s-infra'
    targetRevision: main
    path: apps/knative-nginx-bridge-config 
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: knative-serving
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true # Added for safety against race conditions