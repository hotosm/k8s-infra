# apps/kubeview/helm/values.yaml

rbac:
  create: true

serviceAccount:
  create: true

service:
  type: ClusterIP
  port: 80

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi