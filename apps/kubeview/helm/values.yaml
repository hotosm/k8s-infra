# apps/kubeview/helm/values.yaml

# Chart Service stays internal; tailscale-service.yaml exposes it on the tailnet.
loadBalancer:
  enabled: false

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi
