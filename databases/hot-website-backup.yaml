apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: hot-website-db-backup
  namespace: postgres
spec:
  schedule: "0 0 2 * * *"  # At 02:00 UTC every day
  backupOwnerReference: self
  cluster:
    name: hot-website-db
