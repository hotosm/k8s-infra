apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: central-db-store
  namespace: postgres
spec:
  retentionPolicy: "60d"
  configuration:
    destinationPath: s3://hotosm-k8s-db-backup/central
    endpointURL: https://s3.amazonaws.com
    s3Credentials:
      accessKeyId:
        name: s3-creds
        key: access-key-id
      secretAccessKey:
        name: s3-creds
        key: secret-access-key
    wal:
      compression: gzip # this saves a lot of storage
      encryption: AES256 # AWS managed
    data:
      compression: gzip
      encryption: AES256 # AWS managed
      jobs: 2

---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: central-db
  labels:
    app.kubernetes.io/component: database
    app.kubernetes.io/version: 18-system-trixie

spec:
  instances: 1
  description: "ODK Central Database"
  imageName: "ghcr.io/cloudnative-pg/postgresql:18-system-trixie"

  primaryUpdateStrategy: unsupervised
  enablePDB: true

  postgresql:
    parameters:
      # Kill idle-in-transaction sessions after 60s to release locks held by abandoned txns.
      idle_in_transaction_session_timeout: "60s"

  bootstrap:
    initdb:
      database: central
      owner: central
      secret:
        # Kubernetes basic-auth convention has keys:
        # - username
        # - password
        name: central-db-creds

  storage:
    storageClass: gp3
    size: 2Gi
  walStorage:
    storageClass: gp3
    size: 2Gi

  # prometheus
  monitoring:
    enablePodMonitor: false

  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: central-db-store

---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: central-db-backup
  namespace: postgres
spec:
  schedule: "0 0 5 * * 0"  # At 05:00 UTC every Sunday
  backupOwnerReference: self
  cluster:
    name: central-db
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
