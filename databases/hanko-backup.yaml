apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: hanko-db-backup
  namespace: postgres
spec:
  schedule: "0 0 0 * * *"  # At midnight every day
  backupOwnerReference: self
  cluster:
    name: hanko-db
